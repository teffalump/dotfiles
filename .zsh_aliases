alias v='vim'
alias sv='sudo vim'
alias back='cd -'
alias ls='ls --color=auto'
alias c='clear'
alias grep='grep --color'
alias df='df -h'
alias du='du -h'
alias tree="find . -print | sed -e 's;[^/]*/;|____;g;s;____|; |;g'"
alias nocomment='grep -Ev '\''^(#|$)'\'''
alias wget='wget -c'
alias tundown='sudo ip link del'
alias numfiles='ls -A1 | wc -l'

#"nuke lamers from orbit", lol
alias banish="varnishncsa -d | grep \/ip | grep -i wget | awk ‘{ print $1 }’ | sort | uniq | tee -a /etc/hosts.deny"

#telnet server that broadcasts Star Wars Episode IV as animated text
alias star_wars='telnet towel.blinkenlights.nl' 

#telnet nethack game
alias nethack_server='telnet nethack.alt.org'

#alias rwlan="sudo macchanger -A wlan0"
alias flac2ogg='find . -name "*.flac" -print0 | xargs -0 oggenc -q 10'
alias usage="du -h --max-depth=1 | sort -rh"
alias off="xset dpms force off"
alias update="sudo pacman -Syu"
alias uninstall="sudo pacman -Rs"
alias aur_update="yaourt -Syu --devel --aur"
alias img_clean="exiftool -all="
alias install="sudo pacman -S"
alias lvim="vim -c \"normal '0\""
alias poweroff='sudo shutdown -Ph now'
alias restart='sudo shutdown -r now'
alias recent='ls -toh | head -n 20'

#listening ports
alias ports='ss -tulan'

#games
alias wow="wine /games/World\ of\ Warcraft/Wow.exe -opengl"

#remove orphan packages
alias remove_orphans='sudo pacman -Rs $(pacman -Qtdq)'
#some git aliases
alias ga='git add'
alias gp='git push'
alias gl='git log'
alias gs='git status'
alias gd='git diff'
alias gdc='git diff --cached'
alias gm='git commit -m'
alias gma='git commit -am'
alias gb='git branch'
alias gc='git checkout'
alias gra='git remote add'
alias grr='git remote rm'
alias gpu='git pull'
alias gcl='git clone'
